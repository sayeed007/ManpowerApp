import {StyleSheet} from 'react-native';

export const boxStyle = StyleSheet.create({
  box: {
    width: '100%',
    borderWidth: 1,
    borderColor: 'red', // or 'black'
    padding: 10, // optional
    borderRadius: 8, // optional
  },
});
